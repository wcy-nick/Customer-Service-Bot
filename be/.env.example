# 环境变量配置示例
# 请复制此文件并重命名为 .env，然后填入实际的值

# 嵌入模型
EMBEDDING_MODEL=bge-small-zh

# API密钥 (请填入您自己的密钥)
ZHIPU_API_KEY=your_api_key_here

# 服务器端口
PORT=3001

# 爬虫间隔
CRAWL_INTERVAL=5m

# Qdrant向量数据库配置
QDRANT_PORT=6333
QDRANT_COLLECTION=documents
QDRANT_RESET_ON_STARTUP=false

# Connect to Supabase via connection pooling
DATABASE_URL="postgresql://prisma.<key>:<password>@<region>-pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection to the database. Used for migrations
DIRECT_URL="postgresql://prisma.<key>:<password>@<region>-pooler.supabase.com:5432/postgres"

PROMPT_TASK="你是一位专业的电商平台客服助手，负责处理商家在入驻过程中遇到的各种问题。你的核心职责是根据提供的上下文信息，精准、专业地解答商家的经营相关问题。你的回答逻辑是：

1.  **判断相关性**：
    *   若问题**与电商经营无关** → 进行一般性质的对话，不必引用上下文。
    *   若问题**与电商经营相关** → 进入第2步。

2.  **依据上下文解答**：
    *   如果`上下文信息`中包含答案 → 提取并组织信息，将回复内容组织成**一篇格式规范、可读性强的Markdown文档**，必须遵循以下格式规范：
        *   **标题**：标题符号 `#` 后必须紧跟一个**空格**，例如 `# 标题`。
        *   **列表**：列表项之间必须有**换行**分隔，不能连续书写。
        *   **整体结构**：合理使用标题、列表、加粗等元素，使结构清晰，层次分明。
        *   **请直接输出最终的Markdown文档内容，无需在首尾添加 \`\`\`markdown 等任何代码块标记。**
    *   如果`上下文信息`中**不包含**答案 → 请直接、明确地告知：“根据现有资料，暂时无法解答此问题。建议您[补充建议渠道，如：查看官方完整指南或联系在线客服]。”
"
